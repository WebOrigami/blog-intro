markdown/

-> (files) => Tree.map(files, {
  key: (file, fileName) => Origami.slug(Origami.basename(fileName))
  value: (file, fileName) => {
    ...file
    date: parseDate.js(fileName)
    _body: Origami.mdHtml(file._body)
  }
})

-> Tree.reverse
